printf("\n The node which are reachable are DFS:\n");
	// dfs(1);
    // printf("\n");
	// for (i=1;i<=n;i++) {
	// 	if(reach[i])
	// 	   count++;
	// }
	// if(count==n)
	//   printf("\n Graph is connected"); 
    // else
	//   printf("\n Graph is not connected");